node_modules
dist
dist-ssr
*.local

# Tauri
src-tauri/target
src-tauri/gen/schemas

# Model weights (large binaries — downloaded from HF, never committed)
models/*.onnx
models/*.pt
models/_README.md
# Legacy / wrong-tier dictionaries — only ppocrv6_dict.txt (18708 chars, matches
# the small rec head's 18710 classes) is committed so OCR works out of the box.
models/ppocr_keys_v1.txt
models/ppocrv6_tiny_dict.txt
*.onnx

# Test manga pages (copyrighted source art — not for a public repo)
data/

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.log
